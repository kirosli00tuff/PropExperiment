# secrets — the Databento key lives only in .env, never in source
.env
.env.*
*secret*

# python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# data — vendor raw is immutable and paid for; kept out of git by size, never
# regenerated by overwriting. processed is regenerable from raw.
data/vendor/
data/processed/
*.partial
# sealed holdout store (Stage C Task 6): encrypted paid data; the manifest and unlock log in docs/ ARE committed
data/sealed/
